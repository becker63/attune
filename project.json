{
  "name": "workspace",
  "root": ".",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/codex/pnpm.mjs exec oxlint --config root-oxlintrc.json package.json project.json nx.json tsconfig.base.json packages --quiet"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "node scripts/codex/pnpm.mjs exec nx run-many -t lint,typecheck --all",
          "openspec validate --all --no-interactive"
        ],
        "parallel": false
      }
    },
    "policy-fast": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "corepack pnpm exec nx run attune-nx:test",
          "corepack pnpm exec nx run workspace:policy-proof-pressure"
        ],
        "parallel": false
      }
    },
    "policy-architecture": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "corepack pnpm exec nx run attune-nx:test",
          "corepack pnpm exec nx run workspace:policy-proof-pressure"
        ],
        "parallel": false
      }
    },
    "policy-proof-pressure": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/codex/report-proof-pressure-status.mjs"
      }
    }
  }
}
