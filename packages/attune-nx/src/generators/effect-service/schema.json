{
  "$schema": "http://json-schema.org/schema",
  "$id": "EffectServiceGeneratorSchema",
  "title": "Effect Service",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Service name, for example JoernTemplateExecutor."
    },
    "directory": {
      "type": "string",
      "description": "Directory where the service module should be written.",
      "default": "src/effect/services"
    },
    "export": {
      "type": "boolean",
      "description": "Update an index.ts barrel in the service directory.",
      "default": true
    },
    "tag": {
      "type": "string",
      "description": "Effect.Service identifier."
    },
    "operationId": {
      "type": "string",
      "description": "Auditable operation id recorded in the generated package-contract placeholder."
    },
    "operationKind": {
      "type": "string",
      "description": "Canonical Attune operation kind recorded in the generated package-contract placeholder.",
      "default": "command",
      "enum": [
        "codec",
        "query",
        "command",
        "projection",
        "event-facade",
        "atom-family",
        "resource-provider",
        "generator",
        "policy-rule",
        "joern-template"
      ]
    },
    "project": {
      "type": "string",
      "description": "Owning Nx project recorded in Source BOM provenance."
    },
    "generatorVersion": {
      "type": "string",
      "description": "Generator package version recorded in Source BOM provenance."
    },
    "generatorRevision": {
      "type": "string",
      "description": "Generator source revision recorded in Source BOM provenance."
    },
    "openspecChangeId": {
      "type": "string",
      "description": "OpenSpec change id recorded in Source BOM provenance."
    }
  },
  "required": ["name"],
  "additionalProperties": false
}
