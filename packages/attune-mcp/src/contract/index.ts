/**
 * Public MCP schemas and deterministic contract generation.
 *
 * @remarks
 * This boundary is authoritative for wire compatibility. Lifecycle semantics
 * live under `investigation`; server registration must not mutate these
 * schemas.
 *
 */

export * from "../v0/contract-bundle.js";
export * from "../v0/contracts.js";
