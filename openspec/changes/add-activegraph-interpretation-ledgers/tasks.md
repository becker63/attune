## 1. Consumer-Owned Ledger Contract

- [x] 1.1 Add the frozen `InterpretationLedger` and `LedgerReference` values,
      non-empty field validation, stable content digest, optional supersession,
      `Result.retained_ledger_refs`, and `Packet.ledgers`.
- [x] 1.2 Add the case-bound deterministic `record_interpretation` ActiveGraph
      tool and rebuildable ledger-reference-to-packet-digest projection in a
      focused research module.
- [x] 1.3 Export the consumer contract from `attune_activegraph.research` and
      include the ledger schemas in the pinned common-tool digest.

## 2. ActiveGraph Research Pack

- [x] 2.1 Compose `record_interpretation` into every capability profile while
      preserving the exact eight-tool treatment delta, and raise the bounded tool
      turn allowance so returned ledger references can precede dependent calls.
- [x] 2.2 Make `InvestigationOutput` select `supports` or `challenges`, merge
      ledger references into `Evidence.refs`, and retain surviving references in
      `Result`.
- [x] 2.3 Correct the Case-event filters and handlers for ActiveGraph 1.10's
      event payload and constrained `BehaviorGraph` mutation API.
- [x] 2.4 Update investigate/synthesize prompts and the prose-control
      projection so every arm uses the same bookkeeping while prose receives only
      flattened retained, omitted, assumption, and limitation text.

## 3. Runtime and Boundary Evidence

- [x] 3.1 Add focused Python tests for closed/frozen validation, digest
      sensitivity, case rejection, deterministic tool metadata, immutable
      supersession, packet embedding/indexing, and prose redaction.
- [x] 3.2 Exercise supports/challenges and ledger-reference persistence through
      ActiveGraph-compatible Case handlers, and verify conventional/treatment tool
      parity plus the exact eight-wrapper delta.
- [x] 3.3 Prove a ledger reference passes unchanged through the generated
      Python Attune wrapper and is persisted verbatim in MCP `request.json` and
      `references.json` without changing the wire contract.

## 4. Mechanical Documentation

- [x] 4.1 Add the full-width source-authored `ActiveGraph` chapter between
      `The model` and `The tools`, link the canonical `FreeFormReference`, and
      update reader/compiler/navigation/clear-boundary contracts without adding
      visual chrome or another code fence.
- [x] 4.2 Replace the payment example's decorative constants and receipt/event
      references with three separate executable Python ActiveGraph
      `record_interpretation` `ToolCall` illustrations whose returned digests feed
      the compiler-checked TypeScript MCP requests without inventing a TypeScript
      ActiveGraph client. This completed iteration is superseded by 4.4.
- [x] 4.3 Update semantic fixtures, exact chapter/fence/link expectations,
      documentation budgets, malformed-input coverage, and responsive browser
      journeys for the new chapter and ledger handoff.
- [x] 4.4 Replace the superseded three payment-specific Python continuations
      with one condensed source-faithful `make_research_pack` /
      `make_interpretation_tool` declaration, then continue the tool narrative
      through exact receipt-returned `joern-output.json`, Maude `stdout.txt`,
      property `counterexample.json` / `run-details.json`, ast-grep
      `findings.jsonl`, and the candidate input rule tracked in the initial
      fixture; promote receipt-complete `property.ts` to
      `repo/payment-retry.property.ts`, use
      `repository_checkpoint(policy: "commit")` to create
      `RESEARCH_SNAPSHOT`, and scan that snapshot through eight real MCP calls
      in seven TypeScript fences without inventing a raw mount write,
      `activegraph.call`, or ledger side effect.
- [x] 4.5 Update semantic fixtures and compiler invariants to reject hard-coded
      ledger digests, invented `attune:joern` / `attune:maude` /
      `attune:property` references, `joern.summary`, generic `result.json`
      evidence, the superseded three-Python-fence contract, and false claims
      that repository authoring is an MCP or ActiveGraph operation.

## 5. Validation and Preview

- [x] 5.1 Run Python format, lint, type checking, contract/schema/budget checks,
      research tests, MCP tests, TypeScript formatting/lint/type checks, and strict
      OpenSpec validation.
- [x] 5.2 Rebuild the deterministic documentation artifact, run all unit and
      Playwright journeys across representative viewport/zoom states, visually
      inspect the ActiveGraph/tools/Packet transition, and restart the local
      preview for editorial acceptance.
- [x] 5.3 Re-run Python documentation regressions, guide/compiler validation,
      strict OpenSpec validation, the deterministic build, responsive browser
      journeys, and live-preview inspection for the retained-file continuation
      contract.
