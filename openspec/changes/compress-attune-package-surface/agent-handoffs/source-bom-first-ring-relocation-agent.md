Agent: Codex local implementation
Wave: Real relocation slice 3
Ownership: First Source BOM shard relocation

Changed:
- Added `sourceBomFrameworkShardPath(project)` in `packages/attune-nx/src/internal/source-bom.ts`.
- Extended `scripts/architecture/source-bom-check.mjs` so Source BOM index entries may point to:
  - legacy `<projectRoot>/attune.source-bom.json`
  - local `.attune/cache/source-bom/<project>.json`
  - checked-in `framework/architecture/src/generated/source-bom/<project>.json`
- Moved `attuned-discovery` Source BOM data from
  `packages/attuned-discovery/attune.source-bom.json` to
  `framework/architecture/src/generated/source-bom/attuned-discovery.json`.
- Updated `attune.source-bom.index.json` and `attune.generator-shapes.json` to
  point at the framework-owned projection.
- Updated package-contract target semantics to describe Source BOM projections
  rather than package-local Source BOM as the only read/write surface.
- Updated `compress-attune-package-surface` design and marked task 6.3 done.

Generated:
- `framework/architecture/src/generated/source-bom/attuned-discovery.json` is a
  checked-in framework-owned migration projection. It is not product package
  authored source and should eventually be generated by `attune-repair` or
  projected from ProtocolStore/local cache.

Validated:
- `nx run workspace:source-bom-check --skipNxCache`
- `nx run workspace:shape-conformance --skipNxCache`
- `nx run attune-nx:test --skipNxCache`
- `nx run attune-architecture:test --skipNxCache`
- `nx run workspace:package-contracts-check --skipNxCache`
- `nx run workspace:attune-check --skipNxCache`
- `nx run workspace:attune-repair --dryRun --skipNxCache`
- `openspec validate compress-attune-package-surface --type change`
- `bash -n nix/policy-hooks/touched-source-bom-ownership.sh`
- `git diff --check`

Not run:
- No generated TypeScript companions moved in this slice.
- `workspace:attune-repair` still routes through check/dry-run behavior rather
  than applying Source BOM relocation automatically.

Contract status:
- `attuned-discovery` no longer has package-local `attune.source-bom.json`.
- `attuned-discovery` still has package-local generated TypeScript companions:
  `src/attune.contract.generated.ts` and `src/attune.generated.ts`.

Residual migration debt:
- Move remaining Source BOM shards out of package roots.
- Teach `attune-repair`/materialization to regenerate framework-owned Source BOM
  projections automatically.
- Move generated TypeScript companions or give them explicit framework-owned
  lookup/cache support.
- Ratchet `attune/package-local-surface/one-attune-file` from warning toward
  error after generated companions move.

Blocked by:
- Generated companion relocation still needs reader/import strategy.

Next agent:
- Move package-local generated companions for one low-risk package into a
  framework-owned generated/cache lookup, or wire `attune-repair` to regenerate
  Source BOM projections before moving additional rings.
