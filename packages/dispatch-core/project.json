{
  "name": "dispatch-core",
  "root": "packages/dispatch-core",
  "sourceRoot": "packages/dispatch-core/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "cwd": "packages/dispatch-core",
        "command": "../../node_modules/.bin/tsup src/index.ts --format esm,cjs --dts --sourcemap --clean"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/dispatch-core",
        "command": "../../node_modules/.bin/tsgo --noEmit"
      }
    },
    "typecheck:classic": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/dispatch-core",
        "command": "../../node_modules/.bin/tsc --noEmit"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node_modules/.bin/oxlint --config root-oxlintrc.json packages/dispatch-core --quiet"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/dispatch-core",
        "command": "../../node_modules/.bin/vitest run"
      }
    }
  }
}
