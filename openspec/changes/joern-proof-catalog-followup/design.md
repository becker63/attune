# Design

This follow-up will plan Joern proof work as recipe-backed derivation and
execution:

```text
Generated binding recipe
  -> proof template recipe
  -> bounded campaign ManagedRecipe
  -> normalized observation packet
  -> receipt and health projection
```

The design must keep query generation bounded to approved templates and record
campaign cost, timeout, and proof-pressure evidence.
