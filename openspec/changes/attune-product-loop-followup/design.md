# Design

This follow-up will start from ARS substrate outputs and design the product
loop as recipe-backed workflows:

```text
Repo snapshot
  -> semantic recall recipe
  -> proof recipe
  -> decision recipe
  -> report recipe
  -> receipt and health projection
```

The design must name validation gates for product behavior without treating
large proof catalogs, live platform rollout, or FoldKit UI polish as implicit
requirements.
