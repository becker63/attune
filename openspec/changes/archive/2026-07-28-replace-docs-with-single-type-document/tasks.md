## 1. Prove the seams, then cut cleanly

- [x] 1.1 Install and pin compatible root/docs dependencies for `effect-oxlint`, Effect/platform, `@effect/tsgo`, `@effect/language-service`, TypeScript 7/ts-morph, TSDoc/config, unified/remark/rehype/`rehype-sanitize`/VFile, Shiki, and JSON-RPC/LSP; declare TSDoc/config directly at the root for the root-local plugin, require `remark-parse` in docs for TSDoc CommonMark bodies, and add `remark-gfm` only if authored API tables or a separately selected approved experiment publication require it (no experiment bundle is selected by this change)
- [x] 1.2 Run one disposable pre-cut probe proving real clean-checkout Oxlint TypeScript loading, TSDoc-plus-`remark-parse` preservation of the package CommonMark heading/model list/plain-text diagram/declaration references/canonical first-body-line example title, Oxfmt preservation of that structure and fixed-width diagram, LSP initialization/server requests/cleanup/shutdown, normalized TypeScript/Effect diagnostics, code and synthetic TSDoc declaration references, canonical Effect defaults, context-bound error type parameters, actual Attune Effect/state annotations, virtual inheritance assertions, explicit UTF-16 negotiation, source-to-signature and cut remapping, declaration-map/source resolution for package imports, and Shiki anchor ranges
- [x] 1.3 Read the base revision only to record physical LOC, canonical source owners, and exact deletion targets; do not preserve old routes/page structure or add a migration manifest, parity baseline, policy, converter, or checked-in report
- [x] 1.4 In one replacement change, delete the old `attune-docs` source/tests/static JavaScript, `schema/api-manifest.schema.json`, `docs-policy.json`, `scripts/probe-typedoc.mjs`, and `packages/twoslash`; preserve `schema/experiment-*.schema.json`, then scaffold only `read.ts`, `docs.ts`, `main.ts`, the small stylesheet, focused tests, and root-local lint source
- [x] 1.5 Add artifact guards that reject an `attune-docs-next` package, compatibility adapter, dual renderer/build, route/search/hover artifacts, intermediate JSON authority, browser JavaScript, or surviving Twoslash

## 2. Establish the one-rule TSDoc contract

- [x] 2.1 Add `@microsoft/tsdoc`, its config package, and `tsdoc.json` with standard tags plus only the custom `@failure` block tag; remove legacy `@requires`, `@produces`, `@transitionsTo`, `@moduleDocumentation`, type-expression, and ignore-tag support
- [x] 2.2 Implement one root-local `attune/tsdoc` rule with `effect-oxlint`, including constant/type-value/overload/accessor ownership, direct-or-provisional-inheritDoc comments, meaningful summaries, substantive remarks for exported concepts and their public callable members, summary-only treatment for true leaf properties, exact ordered callable tags, explicit exported/module-level annotations, forbidden filler/type restatements/local ignores, structural validation of virtual-file/error/cut directives, and rejection of TypeScript/Effect diagnostic suppressors inside examples
- [x] 2.3 Replace `.oxlintrc.json` with source-loaded `oxlint.config.ts`; preserve every existing ignore/native-plugin/correctness/platform-neutral invariant, keep `typeAware: true` and `typeCheck: false`, remove duplicative documentation rules and CLI `--type-aware`, add `--disable-nested-config` to lint/fix commands, derive the `attune/tsdoc` override from exact handwritten production roots while excluding the fixed generated convention, reject ignore patterns that match handwritten rule roots, and reject any qualified or all-rule handwritten-production Oxlint/ESLint disable directive that would suppress `attune/tsdoc` during config evaluation
- [x] 2.4 Add focused `effect-oxlint/testing` cases and a real Oxlint CLI TypeScript fixture proving clean-checkout plugin loading, exact source diagnostics, overload/accessor/type-value ownership, required public-callable remarks versus valid summary-only leaf properties, valid standalone inheritDoc, valid/invalid examples, failure-to-diagnostic conversion, generated-source exclusion, suppression/nested-config resistance, and the exact equations `reader = handwritten ∪ generated` and `attune/tsdoc = handwritten`
- [x] 2.5 Enable canonical Oxfmt JSDoc formatting, with a closed config-tested JSDoc-pass exception only for exact TypeScript files containing TSDoc fences because pinned Oxfmt rewrites them into parser-invalid indentation, and bring every production-root `attune-mcp` and `effect-joern` declaration to the finished contract, fixing generated comments at their generator input; convert Effect-error `@throws` to `@failure`, spell public `Attune` errors with the public failure nouns, rewrite package/public TSDoc around authority/action/evidence, replace the 34 current public-owner examples/lenses with one checked complete investigation plus only useful boundary variations and one plain-text lifecycle diagram, reorder the public entrypoint to `Investigation`, `Attune`, receipt, failures, toolkit, preserve lifecycle member order, use the three-part model as the editorial fixture, and keep the unchanged handwritten `attune-mcp` `src`+`test` total at or below 8,000 lines
- [x] 2.6 Update existing Joern generator inputs/templates to emit deterministic TSDoc for every generated production declaration/member; replace mutating `generate` dependencies on Joern build/typecheck/test and the root TypeScript check's upfront generation with nonmutating `joern-effect:generated-check`, retain only the intentional manual regeneration command, and implement that check by regenerating into a temporary directory and comparing the exact relative file set and bytes (currently four files) with tracked working outputs, which the clean publication gate proves equal `HEAD`

## 3. Read source directly into ordinary MDAST

- [x] 3.1 Discover every workspace production `tsconfig.build.json`, enumerate only exact parsed roots, and deterministically order current `attune-mcp`/`joern-effect` packages, files, and source declarations without a manual registry or invented build config
- [x] 3.2 Implement named top-level constants and direct exported-object members plus the six fixed normalization rules: overload family, accessor pair, constructor parameter property, same-scope type/value concept, reexport to narrative owner, and generated comment authority
- [x] 3.3 Parse TSDoc grammar/tags/references with `@microsoft/tsdoc`, parse each owned CommonMark body through always-on `remark-parse`, and lower the package-authored `The model` and `A complete investigation`, direct-reexport/member curriculum order, the sole `text` code node beneath `The model`, declaration headings, exact signatures, semantic sections, links, and each complete example into ordinary MDAST before the deterministic `Repository` tail; write no custom Markdown parser, and let `resolve` mutate each example's one code node to its visible cut value
- [x] 3.4 Add narrow local `data.attune` metadata for role, stable ID, source path/range/href, digests, checked state, and resolved ranges; use friendly public fragments and deterministic package/path/symbol fragments elsewhere
- [x] 3.5 Prove exact production-root/declaration completeness, merged facet destinations, and deterministic MDAST node order/metadata across two in-memory reads without exporting or serializing an inventory, manifest, snapshot, graph, policy, disposition, audience tier, or checked-in debug JSON

## 4. Resolve and check through one unified compiler

- [x] 4.1 Pin `@effect/language-service` plugin options in the actual shared/production and derived example tsconfigs; acquire `@effect/tsgo` with `get-exe-path`, run its TypeScript-Go binary once with `--lsp --stdio`, and implement bounded server/client JSON-RPC lifecycle, cancellation, didClose cleanup, asserted UTF-16 conversion, and exact compiler metadata
- [x] 4.2 Implement the one asynchronous `resolve` transform: partition virtual files, normalize diagnostic source/severity/code/ranges, check/resolve complete projects, authenticate explicit Effect/state syntax, resolve code/reexports and synthetic TSDoc references, bind generic failure targets to owning type-parameter slots, prove inheritDoc with heritage/names/bidirectional assignment, then remap extracted signatures and cuts
- [x] 4.3 Implement the one error-severity unified `check` pass with VFile messages for exact root completeness, unique IDs, local links, inheritance/error predicates, exactly one complete package investigation, its causal lifecycle order/current authority and public-section reference to that one program, every additionally authored example, `@failure` target equality and nonempty explanations, generated summary/callable-tag/link obligations after upstream drift, source spans/digests/revisions, and immutable source links; do not duplicate handwritten source-rule checks or add a per-declaration example quota
- [x] 4.4 Keep local authoring obligations exclusively in `attune/tsdoc`; report semantic defects against their real TypeScript source/range and use only one infrastructure `DocsError` for `read`, `compile`, and `write`
- [x] 4.5 Add the representative unified fixture covering source-authored public curriculum order, all six public `Attune` member signatures, one complete running program with rejected-materialization narrowing, receipt-status inspection, post-execution authority, real finalization input, and visible resolved `Attune`/`Investigation<"active">`/`AttuneReceipt`/lifecycle-member occurrences, public-section references to its one anchor, one focused invalid-state variation using the same vocabulary, a type/value pair, omitted canonical `never` defaults, a generic error parameter, internal helper, reexport, inherited member, generated Joern declaration, multi-file/cut behavior, diagnostic filtering/suppression rejection, astral/combining Unicode, unsupported channel form, signature remapping, and byte determinism

## 5. Lower one static document and replace the tests

- [x] 5.1 Lower ordinary MDAST through remark-rehype with explicit heading and Shiki code handlers, then `rehype-sanitize` with a narrow renderer-owned schema for canonical IDs, source/fragment links, Shiki classes/styles, and symbol markers; forbid raw HTML/unsafe URI schemes and revalidate link closure after sanitation
- [x] 5.2 Render exactly one `Attune` h1 and the Elm-style chapter sequence `The model` → `A complete investigation` → `Investigation<State>` → `Attune` lifecycle → `AttuneReceipt` → `Failures` → `AttuneToolkit` → `Repository`; render the running program and diagram once, have later public sections refer to the one `#complete-investigation` anchor without reverse occurrence IDs, place every remaining production declaration exactly once beneath `Repository`, and render package/file provenance there as non-heading labels
- [x] 5.3 Replace CSS with at most 350 lines for a quiet technical-book layout: one restrained prose column, wider/scrollable exact code, readable prose and monospace stacks, generous rhythm, quiet links, small source links, minimal borders, focus styles, `scroll-margin-top`, and visible `:target`; project only the eight guide chapter headings in compact sticky contents and emit no cards/grids, compiler-facing chapter chrome, client JavaScript, or declaration inventory sidebar
- [x] 5.4 Add the fast HTML contract for exact chapter/contents order, one h1, one complete-investigation node, one diagram node, resolved causal events/current authority and visible core links, public-section references to the one program anchor, both errors under `Failures`, toolkit afterward, `Repository` containment and declaration uniqueness, absent reverse occurrence IDs/cards/inventory navigation/reader-facing compiler headings, exact local href/id closure including private names, immutable source links, relative/safe assets, checked-code markers, source-range bounds, obsolete-artifact absence, and deterministic double builds
- [x] 5.5 Replace the browser matrix with one short Playwright journey that clicks `Investigation` from the opening complete-investigation program, asserts the fragment and computed target styling, uses browser Back to return to that program, and inspects the immutable source href without live navigation
- [x] 5.6 Enforce the selected disabled-publication case: no approved experiment bundle, adapter, namespace, or output exists, and API HTML/CSS has no experiment input; leave any future approved Markdown adapter to a separate change
- [x] 5.7 Perform the public-spine editorial review on the rendered artifact: verify that each section deepens or bounds the same authority/action/evidence model, prose stays adjacent to its formal evidence, restart/recovery remain variations of the running investigation, and no disconnected six-noun catalog, duplicate explanation, or compiler architecture leaks into the chapter; do not replace this judgment with word counts or prose scores

## 6. Wire, enforce, publish, and retire

- [x] 6.1 Define one explicit uncached `attune-docs:build` Nx target requiring a clean committed tree and depending on `attune:lint`, `attune-mcp:build`, `joern-effect:build`, and nonmutating `joern-effect:generated-check`; sequence `main.ts`, focused lint/unified/HTML Vitest, then Playwright directly without recursive Nx or a test/build cycle; have Pages invoke only it and remove package-script bypasses plus separate audit/manifest/site/unchecked-render commands
- [x] 6.2 Update Nx shared inputs, direct root/docs dependencies, lockfiles, Nix/Pages, README/editorial commands, `.github/CODEOWNERS`, and source links for `oxlint.config.ts`, `tsdoc.json`, compiler versions, relative assets, allowed-branch deployment, immutable action pins, least-privilege Pages/OIDC permissions, and the one HTML/CSS artifact with experiment publication disabled; designate the documentation-editorial owner for public source owners/compiler/styles and record explicit documentation-architecture owner approval when the LOC report exceeds 1,500, allowing owner-authored change context to supply that approval without claiming GitHub branch protection is configured
- [x] 6.3 Count every production TypeScript line in the docs compiler, root plugin, and `oxlint.config.ts` discovery/integrity guard together; require architecture review above 1,500, expect 2,200–2,500, fail above 2,500, identify the real LSP resolver/`{@inheritDoc}`/Effect semantic checks as the dominant surface against the old 5,497-line stack, fail CSS above 350, and fail any browser JavaScript, Twoslash, compatibility layer, or intermediate JSON product; also run the unchanged `attune-mcp` LOC check with comments included and reject any attempt to raise its 8,000-line limit, reset its baseline, or narrow its `src`+`test` inputs
- [x] 6.4 Before committing the candidate revision, run Oxfmt, root Oxlint, the nonmutating `joern-effect:generated-check`, TypeScript checks, direct lint/unified/HTML fixtures that do not require immutable publication provenance, and OpenSpec strict validation
- [x] 6.5 Commit the candidate revision, then from that clean commit run the uncached `attune-docs:build` (including focused Vitest/Playwright and deterministic rebuild), full workspace/Nix/Pages checks, and exact immutable source/compiler metadata verification; let CI/Pages recreate the deployable output and never stage or push the gitignored `attune-docs/dist` publication output
- [x] 6.6 During archival, apply the surviving deterministic-reference, minimal-API, and experiment-boundary deltas; remove the retired `reference-first-documentation`, `typed-api-documentation`, and `linked-twoslash-examples` capability specs without recreating empty compatibility capabilities
