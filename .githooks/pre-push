#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if command -v nix >/dev/null 2>&1; then
  exec nix develop -c pnpm exec nx run workspace:policy-push
fi

exec pnpm exec nx run workspace:policy-push
