{
  "name": "attune",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "pnpm check:typescript && nx run-many -t contracts:check format:check lint typecheck test build smoke --projects=attune-activegraph",
    "check:typescript": "nx run attune:format:check && nx run attune:lint && nx run joern-effect:generated-check && nx run-many -t typecheck --exclude=attune-activegraph && nx run-many -t test --exclude=attune-activegraph && nx run-many -t build --exclude=attune-activegraph,attune-docs && nx run-many -t smoke --exclude=attune-activegraph && nx run-many -t package:check --exclude=attune-activegraph",
    "format": "oxfmt . && nixfmt flake.nix nix/agentfs.nix nix/joern.nix nix/workspace.nix",
    "format:check": "oxfmt --check . && nixfmt --check flake.nix nix/agentfs.nix nix/joern.nix nix/workspace.nix",
    "graph": "nx graph",
    "lint": "oxlint --disable-nested-config --deny-warnings .",
    "lint:fix": "oxlint --disable-nested-config --deny-warnings --fix ."
  },
  "devDependencies": {
    "@effect/language-service": "0.87.1",
    "@microsoft/tsdoc": "0.16.0",
    "@microsoft/tsdoc-config": "0.18.1",
    "@types/node": "24.13.3",
    "effect": "4.0.0-beta.101",
    "effect-oxlint": "0.3.3",
    "expect-type": "1.4.0",
    "nx": "23.1.0",
    "oxfmt": "0.60.0",
    "oxlint": "1.75.0",
    "oxlint-tsgolint": "7.0.2001",
    "rolldown": "1.2.0",
    "ts-morph": "28.0.0",
    "tsdown": "0.22.14",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=24.11.0"
  },
  "packageManager": "pnpm@11.17.0",
  "nx": {}
}
