# Local pilot scratch

`state-*` directories are ignored local outputs from superseded experiments.
They are not source, fixtures, or a supported capsule format. The V0's
authoritative native fixture is `scripts/golden-investigation.mjs`; future
research-corpus orchestration belongs outside this Effect capability service.
