{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Taskplane task artifact name."
    },
    "directory": {
      "type": "string",
      "description": "Output directory.",
      "default": "taskplane/pi-agent"
    }
  },
  "required": ["name"]
}
