## 1. Author the product narrative

- [x] 1.1 Replace the package summary with the exact three-item value list and expand `The model` through the research problem, ActiveGraph/Attune boundary, native-artifact stance, amortization hypothesis, benchmark caveat, and final three-part mechanical model.
- [x] 1.2 Add the source-authored framing paragraphs before the existing checked `A complete investigation` program without changing its visible TypeScript bytes or single-example contract.
- [x] 1.3 Reorient the first summaries for `Investigation<State>`, `Attune`, `AttuneReceipt`, and `AttuneToolkit`, and update the documentation README to match the new reader journey.

## 2. Generalize the opening contract

- [x] 2.1 Replace the hard-coded opening sentence check with exact MDAST validation for `h1#top`, one adjacent unordered three-item list, strong lead phrases, substantive explanations, and `h2#the-model` as the first structural boundary.
- [x] 2.2 Make the HAST layout collect every pre-model element into `.opening-copy` and strengthen the post-sanitize HTML contract for the title/list/tree shell.
- [x] 2.3 Replace the opening-paragraph CSS with a narrow serif editorial-list treatment using hanging indentation and restrained native rust markers without changing the accepted tree.

## 3. Prove source ownership and responsive behavior

- [x] 3.1 Update unit fixtures and contracts for the real hero list, mutable source wording, invalid opening shapes, resolved links, unchanged heading/program/diagram counts, and stylesheet boundary.
- [x] 3.2 Extend Playwright coverage for the three-item semantic list, serif typography, marker color, opening/model adjacency, desktop/tree containment, mobile flow, zoom, and zero document overflow.
- [x] 3.3 Align the active typography-change artifacts with the opening list terminology while preserving their accepted sizes, palette, font assets, and remaining editorial-acceptance gate.

## 4. Validate and review

- [x] 4.1 Run formatting, lint, TypeScript, package TSDoc/Oxlint, unit, and strict OpenSpec validation.
- [x] 4.2 Rebuild the deterministic five-file publication, run all browser journeys, and inspect desktop, breakpoint, mobile, model-to-program, declaration-summary, and footer captures.
- [x] 4.3 Replace the hero and opening essay with the branch/root/cutting verbal field, retain the amortization hypothesis and literal mechanical boundary, and keep the checked program bytes unchanged.
- [x] 4.4 Restore `The model` to ordinary source-authored introductory paragraphs with no Branches/Roots/Cuttings list, rows, or dedicated sections; begin the three relevant paragraphs with bold inline names and place one renderer-owned inline span host with fallback span and canvas immediately after each name.
- [x] 4.5 Extend the existing OGL runtime with exact-mask botanical shader modes, motif-specific calm motion, one shared scheduler, intersection/visibility pausing, reduced-motion freeze, and per-host fallback/recovery.
- [x] 4.6 Keep the smaller hero-list type, let ordinary prose use the available publication width, float each paragraph-child host inline-end at approximately 20–25% on wide viewports, give the later anchors a restrained editorial pause, and return each host to bounded ordinary flow on narrow viewports without frames, sticky positioning, section containers, or overflow.
- [x] 4.7 Extend semantic, sanitizer, unit, shader, and Playwright contracts for three matched prose paragraphs, strong inline names immediately followed by shader spans, inline preformatted fallback spans, natural implementation-defined trunk/root flare, host inventory, isolated wrap/resumed-width behavior, animation lifecycle, and zero overflow.
- [x] 4.8 Run formatting, lint, TypeScript, unit, strict OpenSpec, deterministic five-file build, and all browser journeys; inspect desktop, breakpoint, mobile, reduced-motion, fallback, paragraph wrapping, immediately resumed full-width prose, natural trunk/root flare, and long-page captures.
- [x] 4.9 Strengthen all three companion shaders so deterministic nonzero phases show perceptible topology-preserving geometric sway from their fixed motif anchors while retaining the exact time-zero masks, shared scheduler, reduced-motion freeze, and fallback/recovery behavior.
- [x] 4.10 Replace the breakpoint-hidden fixed-character hero sizing with a simple container-responsive grid/stack whose aspect-fitted fallback and canvas keep the complete tree visible and unclipped across portrait, landscape, short, and zoomed layouts; keep inline companions bounded in their prose without document overflow.
- [x] 4.11 Extend unit, shader, and Playwright coverage across representative phone, phone-landscape, tablet, transition, notebook, and desktop viewports, DPR 1 and 2, and effective 80–200 percent browser zoom; run formatting, lint, TypeScript, unit, strict OpenSpec, deterministic five-file build, browser journeys, and visual inspection.
- [x] 4.12 Add source-authored `The tools` and `The Packet` chapters after `The model`, including repository authority, one coherent native Joern/Maude/fast-check/counterexample/ast-grep chain, mechanical correlation, local lossy projections, semantic-loss boundaries, and the no-universal-IR conclusion.
- [x] 4.13 Extend the reader/compiler contracts for the two structural headings, ten conceptual contents links, ordered unlinked native artifact fences, static language highlighting, and unchanged sole checked lifecycle program and sole semantic text diagram.
- [x] 4.14 Add focused execution/syntax coverage for the native packet, rebuild the deterministic five-file publication, and inspect the new long-form chapters at desktop, tablet, phone, and zoomed measures without horizontal overflow.
- [x] 4.15 Create the private `attune-guide` source package, move the long-form package TSDoc and packet-specific execution regression out of `attune-mcp`, keep the runtime entrypoint as the six-name public schedule, and make the reader combine those owners without changing canonical declaration links.
- [x] 4.16 Replace the toy direct-eval inquiry with a first-class retryable-payment fixture, complete repository/Joern/Maude/property/ast-grep call transcript, exact native artifacts, and explicitly named LLM-authored carry ledgers between every semantic boundary.
- [x] 4.17 Update compiler fixtures and reader contracts for the expanded twelve-fence packet and split ownership while preserving the sole checked lifecycle program, sole semantic text diagram, static unlinked artifact treatment, the original 8,000-line MCP gate, and an independent 900-line guide gate.
- [x] 4.18 Run formatting, lint, TypeScript, unit, strict OpenSpec, deterministic five-file build, native Maude/property/ast-grep validation, responsive browser journeys, and live-preview inspection.
- [x] 4.19 Restructure `attune-guide` TSDoc so `The thesis` precedes `The model`; give the thesis the `A living edge, a durable core` `h3` and approximately 275–325 words of text-only hypothesis prose, then make the model state the ActiveGraph/Attune boundary before source-authored `Branches`, `Roots`, and `Cuttings` `h3` subsections and the existing mechanical model.
- [x] 4.20 Update reader/compiler contracts so `h2#the-thesis` is the opening boundary, the four conceptual `h3` headings receive stable fragments without source apparatus, the contents projects eleven `h2` entries, and each existing botanical host is inserted into its subsection's first ordinary paragraph without a bold synthetic lead label.
- [x] 4.21 Add the wide thesis left-rail/empty-right-rail treatment and constrained single-column collapse while preserving model paragraph wrapping, frameless first-paragraph hosts, the existing masks/runtime/lifecycle, responsive hero behavior, and zero document-level horizontal overflow.
- [x] 4.22 Update semantic, sanitizer, unit, shader, and Playwright fixtures for the new chapter order, thesis word/text-only boundary, empty right rail, eleven-item contents, botanical `h3` order, first-paragraph host inventory, absent bold labels, stable fallbacks, reduced motion, and responsive layout; retain the 900-line guide gate with the measured source/test total at 844 lines.
- [x] 4.23 Run formatting, lint, TypeScript, unit, strict OpenSpec, deterministic five-file build, native packet regressions, browser journeys, and visual inspection across desktop, tablet, phone, reduced-motion, fallback, and representative zoom states.
- [x] 4.24 Revise `The tools` to lead with the replay question; add the ordinary `Observe.`, `Formalize.`, `Falsify.`, and `Enshrine.` prose beats plus small artifact-kind run-ins without changing headings, navigation, wrappers, or the twelve-fence language/order contract.
- [x] 4.25 Rename the generic carry ledgers to `PAYMENT_MODEL_ASSUMPTIONS`, `PAYMENT_PROPERTY_INTERPRETATION`, and `PAYMENT_RULE_RESIDUE`; make the Maude conclusion conditional on its keyed-provider assumption; replace the overconfident ast-grep error with the cautious no-fix `review-retryable-payment-without-operation-key` warning; add the synthesis sentence while retaining the `artifact_promote` / `investigation_finalize` epistemic caveat.
- [x] 4.26 Update guide/compiler fixtures for the new source-authored run-ins, local ledger names, conditional conclusion, warning detector, semantic omissions, unchanged fence inventory, and measured 904-line guide source/test total under its revised 925-line gate; run formatting, lint, TypeScript, unit, strict OpenSpec, native packet regressions, deterministic build, responsive browser journeys, and live-preview inspection. The later ActiveGraph contract first revised this package budget to 1,200 lines for a measured 1,134-line source/test total; the final private-FUSE, artifact-promotion, and checkpoint contract measures 1,190 lines beneath an openly revised 1,300-line maintenance ceiling.
- [x] 4.27 Resolve all six introductory operation links to their canonical production tool declarations; compile the seven TypeScript tool fences as one coherent virtual program; project compiler-resolved production and in-chapter definition links only inside those existing blocks; keep the five native non-TypeScript artifacts static; and enforce the measured 3,284-line compiler beneath an honest 3,340-line gate. The later ActiveGraph compiler contract revised this to 3,384 lines beneath 3,450; the final named private-mount/promotion diagnostics measure 3,649 physical lines beneath an honestly revised 3,800-line ceiling.
- [x] 4.28 Remove the superseded 56-character thesis rail, let every thesis prose block use the full guide width at all supported measures, and update unit, responsive, text-scaling, and strict OpenSpec contracts without adding visual content or a special layout wrapper.
- [x] 4.29 Obtain documentation-editorial acceptance of the three-value opening, full-width text-only thesis, revised model boundary and botanical subsections, paced elaborate tool investigation, packet argument, visibly calm inline animated studies, natural trunk/root flare, flexible publication-width prose rhythm, and responsive hero-tree balance before commit, push, or deployment.
- [x] 4.30 Replace the illustrative TypeScript ActiveGraph façade with three separate payment-specific Python `ToolCall` fences against the real generic `make_interpretation_tool` contract; execute those fences in the Python regression, require their computed digests in the dependent TypeScript references, and revise the exact tool/packet fence order from twelve to fifteen without adding code chrome. This completed iteration is superseded by 4.31.
- [x] 4.31 Replace the superseded payment-specific Python continuations with one condensed source-faithful production `make_research_pack` / `make_interpretation_tool` declaration; carry the investigation through exact receipt-returned native files; keep the candidate ast-grep rule tracked in `EXACT_SNAPSHOT`; promote the receipt-listed complete `property.ts` to `repo/payment-retry.property.ts`, commit the dirty tree as `RESEARCH_SNAPSHOT`, and scan that snapshot through eight real MCP calls in seven TypeScript fences; remove hard-coded ledger digests, invented native-evidence aliases, fake `activegraph.call`, and invented worktree-write operations.
- [x] 4.32 Update guide/compiler/Python fixtures for the single production declaration and retained-file sequence; reject the superseded three-fence contract, `attune:joern` / `attune:maude` / `attune:property` aliases, `joern.summary`, generic `result.json` evidence, and computed ledger literals; re-run format, lint, type checking, strict OpenSpec, native artifact regressions, deterministic build, responsive browser journeys, and live-preview inspection.
- [x] 4.33 Remove the separate `A complete investigation` chapter and package example; insert source-authored `The artifacts` between `ActiveGraph` and `The tools` with one investigation AgentFS capsule, operation-scoped validated FUSE acquisition, private `repo/` and `artifacts/` siblings, terminal-drain unmount and persistent copy-up/whiteout remount, the attached Git branch and explicit promotion/checkpoint boundary, common invocation envelope, representative native files, terminal write order, receipt metadata, byte-completeness boundary, and opaque-ledger-reference ownership.
- [x] 4.34 Replace the compiler, contents, source-link, responsive-browser, README, and OpenSpec contracts for the removed tutorial with strict artifact-layout checks; retain generic example checking and the coherent seven-fence TypeScript tools transcript, then rebuild and visually inspect the local publication before the final commit and deployment.
